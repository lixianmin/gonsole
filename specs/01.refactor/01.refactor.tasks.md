# 重构任务清单

本文档基于 `01.refactor.plan.md` 编译，包含可执行的具体任务。

**文档关系**:
- 行为规范: `01.refactor.spec.md` (BDD/Gherkin)
- 技术方案: `01.refactor.plan.md`
- 执行任务: 本文档

**执行原则**:
1. 严格按照任务顺序执行（考虑依赖关系）
2. **测试先行**：每个任务从编写失败的测试开始（宪法第2.1条）
3. 每个任务完成后必须执行验证步骤
4. 任一验证失败必须回滚并重新评估
5. 使用 `make test` 验证整个重构阶段

---

## 任务总览

```
Phase 1: P1 全局变量修复 (宪法第4.2条)
├── T1.1  [P1.1] globalIdGenerator → Manager.idGenerator
├── T1.2  [P1.5] typeOfBytes 内联
├── T1.3  [P1.2] startProcessTime 包私有函数
├── T1.4  [P1.4] subUnsubNames 配置对象化
└── T1.5  [P1.3] Git编译信息 Getter封装

Phase 2: P2 错误处理 (宪法第4.1条)
├── T2.1  [P2.2.1] conn.Close() 显式忽略+注释
├── T2.2  [P2.2.2] ReadTailLines 返回error
├── T2.3  [P2.2.3] session.Handshake() 错误日志
└── T2.4  [P2.2.4] 正则编译错误处理

Phase 3: P3 表格驱动测试重构 (宪法第2.2条)
├── T3.1  [P3.2] tools_test.go 表格驱动
├── T3.2  [P3.2] jwtx/jwt_test.go 表格驱动
├── T3.3  [P3.2] tools/file_test.go 表格驱动+临时文件
└── T3.4  [P3.2] road/session_test.go 表格驱动

Phase 4: P4 Console职责拆分 (宪法第4.3条)
├── T4.1  [P4] 创建 CommandManager 组件
├── T4.2  [P4] 创建 TopicManager 组件
└── T4.3  [P4] Console 改为 Facade 模式
```

---

## Phase 1: P1 全局变量修复

### T1.1 [P1.1] globalIdGenerator → Manager.idGenerator

**优先级**: P0
**预估工时**: 30分钟
**依赖**: 无
**对应规范**: spec.md Feature "消除全局变量" - Scenario "Session ID生成器显式创建"

#### 执行步骤（TDD）

1. **编写测试** - 先写一个测试验证globalIdGenerator被移除：
   ```go
   // road/manager_test.go - 新增测试
   func TestManagerSessionIdGeneration(t *testing.T) {
       tests := []struct {
           name    string
           wantIds []int64
       }{
           {"递增ID", []int64{1, 2, 3}},
       }
       // ... 验证ID生成逻辑
   }
   ```

2. **修改 `road/manager.go`**:
   - 在 `Manager` 结构体中添加字段：`idGenerator atomic.Int64`
   - 修改 `NewSession` 方法，使用 `my.idGenerator.Add(1)` 生成ID
   - 将ID作为参数传递给 `newSession`

3. **修改 `road/session_impl.go`**:
   - 删除包级变量：`var globalIdGenerator atomic.Int64`
   - 修改 `newSession` 函数签名：`func newSession(manager *Manager, link intern.Link, id int64) Session`
   - 直接使用传入的 `id` 参数

4. **运行测试** - 确保 `make test` 通过

#### 代码变更示例

```go
// road/manager.go
type Manager struct {
    // ... 现有字段
    idGenerator atomic.Int64  // 新增
}

func (my *Manager) NewSession(link intern.Link) Session {
    var id = my.idGenerator.Add(1)
    return newSession(my, link, id)
}
```

```go
// road/session_impl.go
// 删除: var globalIdGenerator atomic.Int64

func newSession(manager *Manager, link intern.Link, id int64) Session {
    // ... 使用传入的id，不再自己生成
    var my = &sessionWrapper{&sessionImpl{
        id: id,  // 直接使用参数
        // ...
    }}
}
```

#### 验证标准

- [ ] `make test` 通过
- [ ] `grep -n "globalIdGenerator" road/*.go` 无结果
- [ ] 所有 Session ID 仍保持唯一递增

#### 回滚方案

```bash
git checkout -- road/manager.go road/session_impl.go
```

---

### T1.2 [P1.5] typeOfBytes 内联

**优先级**: P0
**预估工时**: 15分钟
**依赖**: 无
**对应规范**: spec.md Feature "消除全局变量" - Scenario "反射类型缓存封装到结构体"

#### 执行步骤

1. **编写测试** - 验证 `[]byte` 类型处理正常：
   ```go
   func TestUnmarshalRequestArg_ByteType(t *testing.T) {
       tests := []struct {
           name    string
           reqType reflect.Type
           payload []byte
           want    any
       }{
           {"byte类型直接返回", reflect.TypeOf(([]byte)(nil)), []byte("test"), []byte("test")},
       }
       // ...
   }
   ```

2. **修改 `road/session.receive.go`**:
   - 删除包级变量：`var typeOfBytes = reflect.TypeOf(([]byte)(nil))`
   - 在 `unmarshalRequestArg` 函数内直接使用内联表达式

3. **运行测试**

#### 代码变更示例

```go
// road/session.receive.go
// 删除: var typeOfBytes = reflect.TypeOf(([]byte)(nil))

func unmarshalRequestArg(handler *component.Handler, serde serde.Serde, payload []byte) (any, error) {
    // 修改前: if handler.RequestType == typeOfBytes
    if handler.RequestType == reflect.TypeOf(([]byte)(nil)) {
        return payload, nil
    }
    // ...
}
```

#### 验证标准

- [ ] `make test` 通过
- [ ] `grep -n "typeOfBytes" road/*.go` 无结果
- [ ] `[]byte` 类型参数的反序列化行为不变

---

### T1.3 [P1.2] startProcessTime 包私有函数

**优先级**: P1
**预估工时**: 15分钟
**依赖**: 无
**对应规范**: spec.md Feature "消除全局变量" - Scenario "进程启动时间通过依赖注入获取"

#### 执行步骤

1. **编写测试** - 验证启动时间可访问：
   ```go
   func TestProcessStartTime(t *testing.T) {
       got := getProcessStartTime()
       if got.IsZero() {
           t.Error("processStartTime should not be zero")
       }
   }
   ```

2. **修改 `beans/init.go`**:
   - 变量重命名为 `processStartTime`（小写）
   - 添加包私有函数 `getProcessStartTime()`

3. **修改 `beans/topic_top.go:62`**:
   - 调用方式改为 `time.Since(getProcessStartTime())`

4. **运行测试**

#### 代码变更示例

```go
// beans/init.go
var processStartTime = time.Now()  // 改为小写

func getProcessStartTime() time.Time {  // 包私有函数
    return processStartTime
}
```

```go
// beans/topic_top.go:62
// 修改前: body.UpTime = timex.FormatDuration(time.Now().Sub(startProcessTime))
body.UpTime = timex.FormatDuration(time.Since(getProcessStartTime()))
```

#### 验证标准

- [ ] `make test` 通过
- [ ] `go build` 成功
- [ ] `/top` 命令正常显示运行时间

---

### T1.4 [P1.4] subUnsubNames 配置对象化

**优先级**: P1
**预估工时**: 30分钟
**依赖**: 无
**对应规范**: spec.md Feature "消除全局变量"

#### 执行步骤

1. **编写测试** - 验证配置数据正确：
   ```go
   func TestBuiltinSubCommands(t *testing.T) {
       tests := []struct {
           name     string
       }{
           {"sub"},
           {"unsub"},
       }
       // ...
   }
   ```

2. **修改 `consts.go`**:
   - 删除原有的3个 `var` 切片定义
   - 添加 `builtinCommand` 结构体
   - 添加 `builtinSubCommands` 变量
   - 添加 `GetBuiltinSubCommands()` 函数

3. **修改使用处**（查找所有使用 `subUnsubNames` 等的代码）

4. **运行测试**

#### 代码变更示例

```go
// consts.go
// 删除:
// var subUnsubNames = []string{"sub", "unsub"}
// var subUnsubExamples = []string{"sub top", "unsub top"}
// var subUnsubNotes = []string{"订阅主题", "取消订阅主题"}

type builtinCommand struct {
    Name    string
    Example string
    Note    string
}

var builtinSubCommands = []builtinCommand{
    {Name: "sub", Example: "sub top", Note: "订阅主题"},
    {Name: "unsub", Example: "unsub top", Note: "取消订阅主题"},
}

func GetBuiltinSubCommands() []builtinCommand {
    result := make([]builtinCommand, len(builtinSubCommands))
    copy(result, builtinSubCommands)
    return result
}
```

#### 验证标准

- [ ] `make test` 通过
- [ ] `sub`/`unsub` 命令功能正常
- [ ] 原变量名已不存在

---

### T1.5 [P1.3] Git编译信息 Getter封装

**优先级**: P1
**预估工时**: 20分钟
**依赖**: 无
**对应规范**: spec.md Feature "消除全局变量"

#### 执行步骤

1. **编写测试** - 验证GetBuildInfo返回正确：
   ```go
   func TestGetBuildInfo(t *testing.T) {
       info := GetBuildInfo()
       if info.BranchName == "" {
           t.Log("BranchName empty (expected in dev build)")
       }
   }
   ```

2. **修改 `consts.go`**:
   - 添加 `BuildInfo` 结构体
   - 添加 `GetBuildInfo()` 函数

3. **可选**: 修改 `console.go:98-102` 使用 `GetBuildInfo()`

4. **运行测试**

#### 代码变更示例

```go
// consts.go
type BuildInfo struct {
    BranchName    string
    CommitId      string
    CommitMessage string
    CommitTime    string
    AppBuildTime  string
}

func GetBuildInfo() BuildInfo {
    return BuildInfo{
        BranchName:    GitBranchName,
        CommitId:      GitCommitId,
        CommitMessage: GitCommitMessage,
        CommitTime:    GitCommitTime,
        AppBuildTime:  AppBuildTime,
    }
}
```

#### 验证标准

- [ ] `make test` 通过
- [ ] `make web` 成功
- [ ] 启动日志正常显示Git信息

---

## Phase 2: P2 错误处理

### T2.1 [P2.2.1] conn.Close() 显式忽略+注释

**优先级**: P1
**预估工时**: 15分钟
**依赖**: T1.1
**对应规范**: spec.md Feature "显式错误处理" - Scenario "网络写入错误记录日志并返回"

#### 执行步骤

修改 `road/app.go:77-78`:

```go
// 修改前
if _, err1 := conn.Write(heartbeatBuffer); err1 != nil {
    continue
}

// 修改后
if _, err := conn.Write(heartbeatBuffer); err != nil {
    logo.Info("heartbeat write failed, addr=%s, err=%v", conn.RemoteAddr(), err)
    _ = conn.Close() // 显式忽略：commonLink.Close()永不为error，仅设置isClosed标志
    continue
}
```

#### 验证标准

- [ ] `make test` 通过
- [ ] 代码审查确认注释清晰

---

### T2.2 [P2.2.2] ReadTailLines 返回error

**优先级**: P1
**预估工时**: 45分钟
**依赖**: 无
**对应规范**: spec.md Feature "显式错误处理" - Scenario "文件操作错误完整传递"

#### 执行步骤

1. **编写测试** - 表格驱动测试错误场景：
   ```go
   func TestReadTailLines(t *testing.T) {
       tests := []struct {
           name    string
           path    string
           num     int
           wantErr bool
       }{
           {"文件不存在", "/nonexistent/file.log", 10, true},
           {"无效行数", "test.log", 0, true},
       }
       // ...
   }
   ```

2. **修改 `tools/file.go`**:
   - 函数签名改为返回 `([]string, error)`
   - 所有错误点返回包装错误

3. **修改所有调用方**（查找 `ReadTailLines` 的使用）

4. **修改对应测试**

#### 代码变更示例

```go
// tools/file.go
// 修改前: func ReadTailLines(fullPath string, num int, filter string) []string
func ReadTailLines(fullPath string, num int, filter string) ([]string, error) {
    if num <= 0 {
        return nil, fmt.Errorf("invalid num: %d", num)
    }

    var fin, err = os.Open(fullPath)
    if err != nil {
        return nil, fmt.Errorf("open file %s: %w", fullPath, err)
    }
    defer fin.Close()
    // ...
}
```

#### 验证标准

- [ ] `make test` 通过
- [ ] 所有调用方正确处理error
- [ ] 文件不存在时返回错误而非nil

---

### T2.3 [P2.2.3] session.Handshake() 错误日志

**优先级**: P2
**预估工时**: 10分钟
**依赖**: 无
**对应规范**: spec.md Feature "显式错误处理" - Scenario "握手失败返回具体错误原因"

#### 执行步骤

修改 `road/app.go:95-101`:

```go
// 修改后
var err = session.Handshake()
if err != nil {
    logo.Info("session handshake failed, addr=%s, err=%v", conn.RemoteAddr(), err)
    return
}
```

---

### T2.4 [P2.2.4] 正则编译错误处理

**优先级**: P2
**预估工时**: 10分钟
**依赖**: 无
**对应规范**: spec.md Feature "显式错误处理" - Scenario "编译期错误在初始化时处理"

#### 执行步骤

修改 `beans/command_deadlock_detect.go:39-40`:

```go
// 修改前: var titlePattern, _ = regexp.Compile(`...`)
// 修改后:
var titlePattern, err = regexp.Compile(`goroutine.*\[.*?(\d+) minutes\]:`)
if err != nil {
    panic(fmt.Sprintf("failed to compile title pattern: %v", err))
}
```

---

## Phase 3: P3 表格驱动测试重构

### T3.1 [P3.2] tools_test.go 表格驱动

**优先级**: P2
**预估工时**: 30分钟
**依赖**: 无
**对应规范**: spec.md Feature "表格驱动测试" - Scenario "工具函数测试使用表格驱动"

#### 执行步骤

重构 `TestNilToHtmlTable` 为表格驱动：

```go
func TestToHtmlTable(t *testing.T) {
    tests := []struct {
        name string
        data any
        want string
    }{
        {"nil", nil, ""},
        {"empty struct", struct{}{}, "..."},
        // 更多场景...
    }

    for _, tt := range tests {
        t.Run(tt.name, func(t *testing.T) {
            got := ToHtmlTable(tt.data)
            if got != tt.want {
                t.Errorf("ToHtmlTable() = %v, want %v", got, tt.want)
            }
        })
    }
}
```

---

### T3.2 [P3.2] jwtx/jwt_test.go 表格驱动

**优先级**: P2
**预估工时**: 45分钟
**依赖**: 无
**对应规范**: spec.md Feature "表格驱动测试" - Scenario "JWT测试统一用例格式"

#### 执行步骤

合并 `TestSign` 和 `TestExpiration` 为表格驱动测试：

```go
func TestSignAndParse(t *testing.T) {
    type args struct {
        secretKey  string
        claims     jwt.MapClaims
        expiration time.Duration
    }

    tests := []struct {
        name      string
        args      args
        wantErr   bool
        sleep     time.Duration
    }{
        {"正常签名解析", args{"key", jwt.MapClaims{"id": 123}, time.Hour}, false, 0},
        {"空密钥", args{"", jwt.MapClaims{"id": 123}, time.Hour}, true, 0},
        {"过期令牌", args{"key", jwt.MapClaims{"id": 123}, time.Millisecond}, true, 10 * time.Millisecond},
    }

    for _, tt := range tests {
        t.Run(tt.name, func(t *testing.T) {
            signed, err := Sign(tt.args.secretKey, tt.args.claims, WithExpiration(tt.args.expiration))
            if (err != nil) != tt.wantErr {
                t.Fatalf("Sign() error = %v, wantErr %v", err, tt.wantErr)
            }
            if tt.wantErr {
                return
            }

            if tt.sleep > 0 {
                time.Sleep(tt.sleep)
            }

            _, err = Parse(tt.args.secretKey, signed)
            if (err != nil) != (tt.sleep > 0) {
                t.Errorf("Parse() error = %v, wantErr %v", err, tt.sleep > 0)
            }
        })
    }
}
```

---

### T3.3 [P3.2] tools/file_test.go 表格驱动+临时文件

**优先级**: P2
**预估工时**: 45分钟
**依赖**: T2.2（函数签名变更）
**对应规范**: spec.md Feature "表格驱动测试"

#### 执行步骤

1. 重构 `TestReadFileTail` 使用临时文件和表格驱动
2. 重构 `TestTimeSort` 为表格驱动

```go
func TestReadTailLines(t *testing.T) {
    // 创建临时文件
    tmpfile, err := os.CreateTemp("", "test*.log")
    if err != nil {
        t.Fatal(err)
    }
    defer os.Remove(tmpfile.Name())

    // 写入测试数据
    content := "line1\nline2\nline3\nline4\nline5\n"
    tmpfile.WriteString(content)
    tmpfile.Close()

    tests := []struct {
        name   string
        num    int
        filter string
        want   int // 期望行数
    }{
        {"读取全部", 10, "", 5},
        {"读取部分", 3, "", 3},
        {"带过滤", 10, "line3", 1},
        {"零行数", 0, "", 0},
    }

    for _, tt := range tests {
        t.Run(tt.name, func(t *testing.T) {
            got, err := ReadTailLines(tmpfile.Name(), tt.num, tt.filter)
            if err != nil {
                t.Fatalf("ReadTailLines() error = %v", err)
            }
            if len(got) != tt.want {
                t.Errorf("ReadTailLines() got %d lines, want %d", len(got), tt.want)
            }
        })
    }
}
```

---

### T3.4 [P3.2] road/session_test.go 表格驱动

**优先级**: P3
**预估工时**: 45分钟
**依赖**: 无
**对应规范**: spec.md Feature "表格驱动测试" - Scenario "Session测试覆盖多种场景"

#### 执行步骤

重构 `TestMakeFunc` 为表格驱动，增加异常场景。

---

## Phase 4: P4 Console职责拆分

### T4.1 [P4] 创建 CommandManager 组件

**优先级**: P2
**预估工时**: 60分钟
**依赖**: 无
**对应规范**: spec.md Feature "Console结构体职责分离" - Scenario "命令注册与执行分离"

#### 执行步骤（TDD）

1. **编写测试** `command_manager_test.go`：
   ```go
   func TestCommandManager(t *testing.T) {
       tests := []struct {
           name string
           // ...
       }{
           {"注册命令"},
           {"获取命令"},
           {"获取所有命令"},
       }
       // ...
   }
   ```

2. **创建 `command_manager.go`**:

```go
package gonsole

type CommandManager struct {
    commands sync.Map
}

func NewCommandManager() *CommandManager {
    return &CommandManager{}
}

func (m *CommandManager) Register(cmd *Command) {
    if cmd != nil && cmd.Name != "" {
        m.commands.Store(cmd.Name, cmd)
    }
}

func (m *CommandManager) Get(name string) ifs.Command {
    if box, ok := m.commands.Load(name); ok {
        if cmd, ok := box.(*Command); ok {
            return cmd
        }
    }
    return nil
}

func (m *CommandManager) GetAll() []ifs.Command {
    // ... 原有逻辑
}

func (m *CommandManager) RegisterBuiltins(port int) {
    // ... 原有内置命令注册逻辑
}
```

3. **运行测试** - `CommandManager` 独立测试通过

#### 验证标准

- [ ] `CommandManager` 独立测试通过
- [ ] 命令注册/查找功能正常

---

### T4.2 [P4] 创建 TopicManager 组件

**优先级**: P2
**预估工时**: 60分钟
**依赖**: T4.1
**对应规范**: spec.md Feature "Console结构体职责分离" - Scenario "Topic管理独立封装"

#### 执行步骤

类似 T4.1，创建 `topic_manager.go` 和 `topic_manager_test.go`。

---

### T4.3 [P4] Console 改为 Facade 模式

**优先级**: P2
**预估工时**: 45分钟
**依赖**: T4.1, T4.2
**对应规范**: spec.md Feature "Console结构体职责分离"

#### 执行步骤

1. **修改 `console.go`**:
   - `Console` 结构体改为持有 `*CommandManager` 和 `*TopicManager`
   - 删除 `commands`, `topics` 字段
   - 公共方法保持不变，内部委托给 Manager

```go
type Console struct {
    options      consoleOptions
    app          *road.App
    gpid         string
    baseUrl      string

    commandManager *CommandManager
    topicManager   *TopicManager

    lastAuthTime atomic.Value
}

// RegisterCommand 保持API不变
func (my *Console) RegisterCommand(cmd *Command) {
    my.commandManager.Register(cmd)
}
```

2. **修改 `NewConsole` 初始化逻辑**

3. **运行所有测试**

#### 验证标准

- [ ] 所有现有测试通过
- [ ] 公共API签名不变
- [ ] 功能测试通过（手动验证console页面）

---

## 附录：验证命令

```bash
# 1. 编译测试
make test

# 2. 完整构建
make web

# 3. 运行示例（手动验证）
cd examples && go run demo.go
```

---

## 附录：文档索引

| 文件 | 用途 | 格式 |
|------|------|------|
| `01.refactor.spec.md` | 行为规范 | BDD/Gherkin |
| `01.refactor.plan.md` | 技术方案 | Markdown |
| `01.refactor.tasks.md` | 可执行任务 | 本文档 |

根据宪法第1.1条和第1.2条：
- **spec.md 是单一真理源**：任何需求变更从spec开始
- **plan.md 服务 spec**：技术方案服务于行为规范
- **tasks.md 服务 plan**：执行任务服务于技术方案
